# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: ['*']
  push:
    branches: ['*']

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    name: Build and Test
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.12.11, 2.13.2]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Cache ivy2
        uses: actions/cache@v1
        with:
          path: ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (generic)
        uses: actions/cache@v1
        with:
          path: ~/.coursier/cache/v1
          key: ${{ runner.os }}-generic-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (linux)
        if: contains(runner.os, 'linux')
        uses: actions/cache@v1
        with:
          path: ~/.cache/coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (macOS)
        if: contains(runner.os, 'macos')
        uses: actions/cache@v1
        with:
          path: ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (windows)
        if: contains(runner.os, 'windows')
        uses: actions/cache@v1
        with:
          path: ~/AppData/Local/Coursier/Cache/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache sbt
        uses: actions/cache@v1
        with:
          path: ~/.sbt
          key: ${{ runner.os }}-sbt-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Setup Ruby
        uses: actions/setup-ruby@v1
        with:
          ruby-version: 2.7

      - name: Install Jekyll
        run: gem install jekyll -v 4.0.0

      - name: Check that workflows are up to date
        run: sbt ++${{ matrix.scala }} githubWorkflowCheck

      - run: sbt ++${{ matrix.scala }} ci

      - name: Upload target directory 'target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-target
          path: target

      - name: Upload target directory 'docs/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-docs_target
          path: docs/target

      - name: Upload target directory 'core/.js/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-core_.js_target
          path: core/.js/target

      - name: Upload target directory 'laws/.js/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-laws_.js_target
          path: laws/.js/target

      - name: Upload target directory 'core/.jvm/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-core_.jvm_target
          path: core/.jvm/target

      - name: Upload target directory 'tests/.js/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-tests_.js_target
          path: tests/.js/target

      - name: Upload target directory 'laws/.jvm/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-laws_.jvm_target
          path: laws/.jvm/target

      - name: Upload target directory 'tests/.jvm/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-tests_.jvm_target
          path: tests/.jvm/target

      - name: Upload target directory 'project/target'
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-project_target
          path: project/target

  publish:
    name: Publish Artifacts
    needs: [build]
    if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/master')
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.2]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Cache ivy2
        uses: actions/cache@v1
        with:
          path: ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (generic)
        uses: actions/cache@v1
        with:
          path: ~/.coursier/cache/v1
          key: ${{ runner.os }}-generic-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (linux)
        if: contains(runner.os, 'linux')
        uses: actions/cache@v1
        with:
          path: ~/.cache/coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (macOS)
        if: contains(runner.os, 'macos')
        uses: actions/cache@v1
        with:
          path: ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (windows)
        if: contains(runner.os, 'windows')
        uses: actions/cache@v1
        with:
          path: ~/AppData/Local/Coursier/Cache/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache sbt
        uses: actions/cache@v1
        with:
          path: ~/.sbt
          key: ${{ runner.os }}-sbt-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Download target directory 'target' (2.12.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.11-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'target' (2.13.2)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.2-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'docs/target' (2.12.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.11-${{ matrix.java }}-docs_target
          path: docs/target

      - name: Download target directory 'docs/target' (2.13.2)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.2-${{ matrix.java }}-docs_target
          path: docs/target

      - name: Download target directory 'core/.js/target' (2.12.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.11-${{ matrix.java }}-core_.js_target
          path: core/.js/target

      - name: Download target directory 'core/.js/target' (2.13.2)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.2-${{ matrix.java }}-core_.js_target
          path: core/.js/target

      - name: Download target directory 'laws/.js/target' (2.12.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.11-${{ matrix.java }}-laws_.js_target
          path: laws/.js/target

      - name: Download target directory 'laws/.js/target' (2.13.2)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.2-${{ matrix.java }}-laws_.js_target
          path: laws/.js/target

      - name: Download target directory 'core/.jvm/target' (2.12.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.11-${{ matrix.java }}-core_.jvm_target
          path: core/.jvm/target

      - name: Download target directory 'core/.jvm/target' (2.13.2)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.2-${{ matrix.java }}-core_.jvm_target
          path: core/.jvm/target

      - name: Download target directory 'tests/.js/target' (2.12.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.11-${{ matrix.java }}-tests_.js_target
          path: tests/.js/target

      - name: Download target directory 'tests/.js/target' (2.13.2)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.2-${{ matrix.java }}-tests_.js_target
          path: tests/.js/target

      - name: Download target directory 'laws/.jvm/target' (2.12.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.11-${{ matrix.java }}-laws_.jvm_target
          path: laws/.jvm/target

      - name: Download target directory 'laws/.jvm/target' (2.13.2)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.2-${{ matrix.java }}-laws_.jvm_target
          path: laws/.jvm/target

      - name: Download target directory 'tests/.jvm/target' (2.12.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.11-${{ matrix.java }}-tests_.jvm_target
          path: tests/.jvm/target

      - name: Download target directory 'tests/.jvm/target' (2.13.2)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.2-${{ matrix.java }}-tests_.jvm_target
          path: tests/.jvm/target

      - name: Download target directory 'project/target'
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-project_target
          path: project/target

      - name: Publish project
        run: sbt ++${{ matrix.scala }} +publish